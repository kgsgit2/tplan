'use client'

interface PlaceSearchProps {
  onSelectPlace: (place: any) => void
  currentLocation?: string
}

export default function KakaoPlaceSearch({ onSelectPlace, currentLocation }: PlaceSearchProps) {
  return null // 임시로 null 반환
}